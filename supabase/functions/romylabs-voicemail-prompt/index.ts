import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
serve(async()=>{const base=`${Deno.env.get('SUPABASE_URL')}/functions/v1`;const xml=`<?xml version="1.0" encoding="UTF-8"?><Response><Say voice="openai.alloy" language="en-US">We can't get to the phone right now. Please leave your name, number, and a short message after the tone. Press pound when you're finished.</Say><Record action="${base}/romylabs-voicemail-recorded" maxLength="180" finishOnKey="#" playBeep="true"/></Response>`;return new Response(xml,{headers:{'Content-Type':'text/xml'}})})
